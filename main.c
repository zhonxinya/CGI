#include "html_lib.h"
#include<stdlib.h>
#include "sdata.h"
#include<string.h>

int main(){
    init();
    // Msg msg1={"name","hello",NULL};
    // Msg msg2={"age","12",NULL};
    // char value[20];
    // struct list_head  head, *pos;
    // INIT_LIST_HEAD(&head);
    // list_add(&(msg1.list),&head);
    // list_add(&(msg2.list),&head);
    // free(find_key("name",&head));
    return 0;
}
//找到链表中符合的值




